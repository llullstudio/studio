Small mono link with an auto-appended amber arrow — for outbound URLs on cards, metadata lines, and footers.

```jsx
<MonoLink href="https://rllull.substack.com" target="_blank">rllull.substack.com</MonoLink>
```

Notes: the " →" suffix is added via CSS; don't include an arrow in children.
