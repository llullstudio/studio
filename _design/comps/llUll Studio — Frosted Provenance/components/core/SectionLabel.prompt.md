Mono uppercase letterspaced label that opens a page section — use above any grid or content block.

```jsx
<SectionLabel>Imprints</SectionLabel>
```

Notes: faint color by default (--text-faint); typically followed by 1.8rem of space before content.
